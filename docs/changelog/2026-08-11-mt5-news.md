# 2026-08-11 - Source-backed MT5 news evaluation

## What changed

- Added exact FBS server-time conversion and strict parsers for raw MT5
  calendar exports.
- Added a read-only MQL5 USD/XAU calendar exporter and guarded Windows runner
  for the approved FBS terminal.
- Added atomic normalization, provenance manifests, canonical strategy-session
  generation, coverage audits, and CLI entry points.
- Replaced the header-only `data/news_blackouts.csv` with the verified
  source-backed calendar output.
- Reran frozen Grid v1.0 and Pullback v1.1 without parameter changes.

## Evidence

The completed export used terminal build 6090 on `FBS-Demo` and recorded
`application_can_trade=false`. All 158 expected monthly USD/XAU queries
succeeded and yielded 24,596 unique raw rows. The canonical news file contains
8,098 normalized rows and has SHA-256
`72c310660954965f13f3394f30273e6a5349647bbce3b6865dba6109be20e26d`.

Coverage is complete for all 4,056 Grid and 6,084 Pullback candidate sessions.
Independent reconciliation matched query identities, counts, hashes, and the
generated coverage classification exactly.

## Outcome

- Grid v1.0: `development_rejected`; neither of two windows passed.
- Pullback v1.1: `development_rejected`; 0 of 36 candidates passed.
- Validation and test were not accessed for either strategy.
- Shadow and demo stages were not started.
- No GSCALP order API was called and no order was submitted.

The terminal rejection path satisfies the research objective for these frozen
versions. Future work, if requested, must start from a separately preregistered
strategy version.
