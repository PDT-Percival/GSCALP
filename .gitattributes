# Normalize line endings: LF in the repo and on checkout for text files.
* text=auto eol=lf

# Windows-only scripts must keep CRLF.
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary artifacts — never touch.
*.png binary
*.jpg binary
*.h5 binary
*.parquet binary
*.zip binary
